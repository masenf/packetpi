# /etc/ax25/ax25d.conf
# {{ ansible_managed }}
# ax25d Configuration File:
#
# generated by ansible from /etc/ax25/ax25d.conf.d/*.conf
# All permanent changes should go there ^ as new files -- changing
#   other ansible managed config files are subject to being overwritten
#
# AX.25 Ports begin with a '['.
# NET/ROM ports begin with a '<'.
#
# These services are generated from the `ax25d` array by the `ax25` role
{% for service in ax25d %}
[{{ service.callsign | d('{{ callsign }}', true) | upper }} VIA {{ service.axport }}]
NOCALL  * * * * * * L
default * * * * * 0 - {{ service.user }} {{ service.program }} {{ service.program_name | d('{{ service.program }}', true) }} {{ service.args | d('', true) }}
{% endfor %}
#
#
{% for service in netrom %}
<{{ service.nrport }}>
NOCALL  * * * * * * L
default * * * * * * - {{ service.user }} {{ service.program }} {{ service.program_name | d('{{ service.program }}', true) }} {{ service.args | d('', true) }}
{% endfor %}

